# Get a list of TUV inputs and their default values

Inputs that don't have a default value and thus are required to be
specified in `set_tup_options()` are shown as an empty vector of the
required data type.

## Usage

``` r
tuv_aq_defaults()
```

## Value

a list of TUV inputs and their default values
